var sim = (function () {

  function init() {
  }
  
  function start() {
  }
  
  function nextDay() {
  }
  
  function getResults() {
  }
  
  return {
    init: init,
    start: start,
    nextDay: nextDay,
    getResults: getResults,
  };
})();

sim.init();
